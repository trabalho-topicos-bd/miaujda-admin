import { Layout } from '../../../components/layout';

export const DashboardPage = (): JSX.Element => (
    <Layout title="Dashboard">
        <div>
            <h1>Dashboard</h1>
        </div>
    </Layout>
);
